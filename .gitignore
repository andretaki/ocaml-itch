# One build directory per opam switch: the base compiler, flambda and OxCaml
# each need their own so they do not clobber each other's artifacts.
_build*/
_opam/
*.install
data/
bench/cpp/baseline

# Python bytecode from the cross-check scripts.
__pycache__/
